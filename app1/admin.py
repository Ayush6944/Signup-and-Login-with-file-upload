from django.contrib import admin
from.models import EBooksModel
admin.site.register(EBooksModel)
# here we are registering our model on django admin database like
# EBooksModel is a model name to store user files
